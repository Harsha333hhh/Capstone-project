{
  "buildCommand": "cd blog-app-frontend && npm install && npm run build",
  "outputDirectory": "blog-app-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
